using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMeleeController : MonoBehaviour
{
    // Use the animator to start the Attack1 animation

    // There should be an animation state that controls when the character is aiming or when they are melee attacking
    // The aiming should only be allowed when the character is not melee attacking
    // Only allow for the collider to be triggered when the character is melee attacking
}
